[pytest]
markers =
    db: test requires PostgreSQL access (real data replay)
    integration: test requires live services (OKX sandbox, Redis, etc.)
    slow: test is slow (>5s), skipped by default in dev loop
