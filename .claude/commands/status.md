Quick project status check. For each service (data, strategy, ai, risk, execution, dashboard): list the main files and note if they exist. Check if Docker containers are running with `docker compose ps`. Check if the bot process is running. Do NOT run pytest (use /test for that). Keep output brief — a table or short list.
